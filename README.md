# Vercel Vite + React Starter (Root-based)

A minimal Vite + React template without a `src/` folder.

## Quick Start

```bash
npm i
npm run dev
# then deploy with Vercel CLI or Git integration
```

- Static build output in `dist`
- Serverless health endpoint at `/api/health`
