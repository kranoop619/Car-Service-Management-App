export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Inter, sans-serif" }}>
      <h1>It works 🎉</h1>
      <p>Minimal Vite + React template (no src/ folder), ready for Vercel.</p>
      <ul>
        <li>Serverless function: <code>/api/health</code></li>
      </ul>
    </div>
  )
}
